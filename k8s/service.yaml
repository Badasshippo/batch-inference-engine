apiVersion: v1
kind: Service
metadata:
  name: batch-inference-engine
  labels: { app: batch-inference-engine }
spec:
  selector: { app: batch-inference-engine }
  ports:
    - name: http
      port: 80
      targetPort: 8080
  type: ClusterIP
